# AWS SDK for Python, used by the Lambda function to interact with AWS services
boto3

# The testing framework used to write and run the unit tests
pytest

# Moto is used for mocking AWS services during the Pytest run.
# Specifying services in brackets installs only the necessary dependencies.
moto[s3,dynamodb,cloudwatch,bedrock,sns]